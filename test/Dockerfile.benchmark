FROM python:3.11-slim

WORKDIR /app

# Create a directory for test tokens
RUN mkdir -p test/

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD ["pytest", "test/test_api_benchmarks.py"]